<!DOCTYPE html>
<html>
  <head>
    <title>Midiframe</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="//vjs.zencdn.net/5.4.6/video-js.min.css" rel="stylesheet">
    <script src="//vjs.zencdn.net/5.4.6/video.min.js"></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', async: true %>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  </head>
  <%# hide content and replace with loading when submit videos %>
  <body>
    <%# data-controller loading, so we can hide form when user is uploading %>
    <div data-controller="loading">
      <h1><%= params[:controller].upcase %></h1>
      <div id="page_content" data-target="loading.hide">
        <%= yield %>
      </div>
    <%# this div below is meant to replace the "page_content" above when a form is laoding. This really helps for UX in long upload times.%>
      <div id="loading_bar" data-target="loading.show" class="hidden"> 
          <div class="progress">
              <div class="indeterminate"></div>
          </div>
      </div>
    </div>
  </body>
</html>
